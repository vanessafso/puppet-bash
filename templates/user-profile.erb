### MANAGED BY PUPPET ###

# Get the aliases and functions
if [ -f ~/.bashrc ]; then
	. ~/.bashrc
fi

# User specific environment and startup programs


<% @env_variables.sort.map do |k,v| -%>
<%= k.upcase %>="<%= v %>"
export <%= k.upcase %>
<% end -%>
